# account-back

"dev": "npx nodemon",
    "build": "tsc -w",
    "test": "jest -c ./jest.unit.json",
    "lint": "npx eslint ./src",
    "format": "npx eslint ./src --fix",
    "build": "tsc",
    "start": "node ./dist",
